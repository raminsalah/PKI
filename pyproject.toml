[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "pkictl"
version = "0.1.0"
description = "PKI workspace management tool"
readme = "README.md"
requires-python = ">=3.10"

[project.scripts]
pkictl = "pkictl.main:main"